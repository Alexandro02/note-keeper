// Here all types will be stored.

export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};

export type Note = {
  id: string;
  category: string;
  content: string;
}